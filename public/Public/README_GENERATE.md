This small script generates individualized HTML pages from the `index.html` template.

Usage (macOS, zsh):

1. Install Node.js if you don't have it.
2. Run:

   node scripts/generate_pages.js

The script writes multiple files into the repository root next to `index.html`.
